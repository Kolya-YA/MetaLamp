ul.fonts-palette
    .font-palette__item.font-palette__item--h1
        .font-palette__name
            h1 H1
        .font-palette__text 
            h1 This one is the sub-section or widget title
    .font-palette__item.font-palette__item--h2
        .font-palette__name
            h2 H2
        .font-palette__text 
            h2 Next one is the item title inside widgets
    .font-palette__item.font-palette__item--h3
        .font-palette__name 
            h3 H3
        .font-palette__text 
            h3 This is a label or CTA text
    .font-palette__item.font-palette__item--body
        .font-palette__name 
            p Body
        .font-palette__text 
            p This is the body text which is used for most of the design, like paragraphs, lists, etc.